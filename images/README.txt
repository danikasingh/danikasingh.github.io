Place your profile image here as `profile.jpg`.

Recommended:
- Filename: profile.jpg
- Size: at least 600px wide
- Output format: JPG or WebP
- Optional: provide a `profile@2x.jpg` for retina using `srcset` if desired.

After adding the image file, the site will display it in the header automatically.